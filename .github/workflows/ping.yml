name: Keep Render Awake

on:
  schedule:
    - cron: '*/5 * * * *' # Har 5 daqiqada ishlaydi
  workflow_dispatch: # Qo'lda ishga tushirish uchun

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s -I https://eco-talim.onrender.com/api/ping
